table:
  name: go1_licenses
  schema: public
configuration:
  column_config:
    enrolled_on:
      custom_name: enrolledOn
    expire_date:
      custom_name: expireDate
    org_id:
      custom_name: orgId
    profile_id:
      custom_name: profileId
  custom_column_names:
    enrolled_on: enrolledOn
    expire_date: expireDate
    org_id: orgId
    profile_id: profileId
  custom_root_fields: {}
object_relationships:
  - name: organization
    using:
      foreign_key_constraint_on: org_id
insert_permissions:
  - role: tt-admin
    permission:
      check: {}
      columns:
        - expire_date
        - org_id
        - profile_id
select_permissions:
  - role: tt-admin
    permission:
      columns:
        - enrolled_on
        - expire_date
        - id
        - org_id
        - profile_id
      filter: {}
  - role: user
    permission:
      columns:
        - enrolled_on
        - expire_date
        - id
        - org_id
        - profile_id
      filter:
        profile_id:
          _eq: X-Hasura-User-Id
delete_permissions:
  - role: tt-admin
    permission:
      filter: {}
event_triggers:
  - name: go1_license_expire_date_changed
    definition:
      enable_manual: true
      insert:
        columns: '*'
      update:
        columns:
          - expire_date
    retry_conf:
      interval_sec: 10
      num_retries: 0
      timeout_sec: 60
    webhook: '{{EVENTS_HOST}}/go1-licensing/schedule-expire'
