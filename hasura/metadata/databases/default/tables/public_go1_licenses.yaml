table:
  schema: public
  name: go1_licenses
object_relationships:
  - name: organization
    using:
      foreign_key_constraint_on: org_id
select_permissions:
  - role: tt-admin
    permission:
      columns:
        - expire_date
        - id
        - org_id
        - profile_id
      filter: {}
