table:
  name: course_participant_cancellation
  schema: public
insert_permissions:
  - role: tt-admin
    permission:
      check: {}
      columns:
        - cancellation_fee_percent
        - cancellation_reason
        - course_id
        - profile_id
select_permissions:
  - role: tt-admin
    permission:
      columns:
        - cancellation_fee_percent
        - course_id
        - cancellation_reason
        - created_at
        - updated_at
        - id
        - profile_id
      filter: {}
