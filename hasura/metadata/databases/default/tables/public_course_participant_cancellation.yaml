table:
  schema: public
  name: course_participant_cancellation
insert_permissions:
  - role: tt-admin
    permission:
      check: {}
      columns:
        - cancellation_fee_percent
        - cancellation_reason
        - course_id
        - profile_id
      backend_only: false
select_permissions:
  - role: tt-admin
    permission:
      columns:
        - cancellation_fee_percent
        - course_id
        - cancellation_reason
        - created_at
        - updated_at
        - id
        - profile_id
      filter: {}
