table:
  schema: public
  name: identity_type
is_enum: true
array_relationships:
  - name: identities
    using:
      foreign_key_constraint_on:
        column: type
        table:
          schema: public
          name: identity
