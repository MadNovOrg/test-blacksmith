table:
  name: course_team
  schema: public
object_relationships:
- name: courses
  using:
    foreign_key_constraint_on: course_id
- name: profile
  using:
    foreign_key_constraint_on: profile_id
select_permissions:
- permission:
    columns:
    - id
    - profile_id
    - course_id
    - type
    filter: {}
  role: user
