table:
  name: course_invites
  schema: public
object_relationships:
- name: course
  using:
    foreign_key_constraint_on: course_id
insert_permissions:
- permission:
    backend_only: false
    check: {}
    columns:
    - course_id
    - email
  role: user
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - id
    - email
    - status
    - created_at
    - updated_at
    - course_id
    filter: {}
  role: user
