table:
  name: course_evaluation
  schema: public
object_relationships:
- name: course
  using:
    foreign_key_constraint_on: course_id
insert_permissions:
- permission:
    backend_only: false
    check: {}
    columns:
    - course_id
    - id
    - signed
  role: user
select_permissions:
- permission:
    columns:
    - course_id
    - id
    - signed
    filter: {}
  role: user
