table:
  schema: public
  name: organization_status
is_enum: true
array_relationships:
  - name: organizations
    using:
      foreign_key_constraint_on:
        column: status
        table:
          schema: public
          name: organization
