- name: allowed-queries
  definition:
    queries:
      - name: GetUserKnowledgehubAccess
        query: |
          query GetUserKnowledgehubAccess ($id: uuid!) {
            profile(where: {id:{_eq:$id}}) {
              canAccessKnowledgeHub
            }
          }
