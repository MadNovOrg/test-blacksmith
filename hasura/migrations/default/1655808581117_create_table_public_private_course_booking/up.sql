CREATE TABLE "public"."private_course_booking" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "created_at" timestamptz DEFAULT now(), "given_name" Text NOT NULL, "family_name" text NOT NULL, "email" text NOT NULL, "phone" text NOT NULL, "org_name" text NOT NULL, "sector" text NOT NULL, "message" text, "source" text, "course_id" integer NOT NULL, "num_participants" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("course_id") REFERENCES "public"."course"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE ("course_id", "email"));COMMENT ON TABLE "public"."private_course_booking" IS E'Private course booking enquiry';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
