CREATE TABLE "public"."course_enquiry" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "created_at" timestamptz DEFAULT now(), "interest" text NOT NULL, "given_name" text NOT NULL, "family_name" text NOT NULL, "email" text NOT NULL, "phone" text NOT NULL, "org_name" text NOT NULL, "sector" text NOT NULL, "message" text, "source" text, "course_id" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("course_id") REFERENCES "public"."course"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE ("email", "course_id"));COMMENT ON TABLE "public"."course_enquiry" IS E'Enquiries about open courses';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
