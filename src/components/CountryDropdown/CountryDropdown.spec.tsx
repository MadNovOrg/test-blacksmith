// TODO: @Ionel to write some tests for the country dropdown component
import { CountryDropdown } from './CountryDropdown'

describe(CountryDropdown.name, () => {
  it('passes', () => {
    expect(true).toBeTruthy()
  })
})
